#include <stdio.h>
#include <stdlib.h>
#include"func.h"
extern int available; //
extern int inOneMore; // declaration
extern int onlyHere;
int main()
{
    long long int num;
    int sod;
    register int itr; // request to the compiler
    num = 1234567890;
    sod = sumOfDigits(num);
    printf("sod = %d\n", sod);
    printf("available = %d\n", available);
    printf("inOneMore = %d\n", inOneMore);
    printf("nod = %d\n", numberOfDigits(num));
    printf("3^5 = %d\n", power(3,5));
    dummy();
    for ( itr=0; itr < 10; itr+=1)
        stat_demo();

        printf("only here =%d\n", onlyHere);
    return 0;
}
